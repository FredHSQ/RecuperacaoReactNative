import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        paddingHorizontal: 20,
        paddingVertical: 20,
        backgroundColor: '#516',
        borderRadius: 10
    },
    buttonText: {
        fontWeight: 'bold',
        color: '#fff',
        fontSize: 15
    }
})